{
  "name": "11ty-doqmeat",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx @11ty/eleventy && npx pagefind --site _site",
    "incr": "npx @11ty/eleventy --serve --incremental && start https://localhost:8080",
    "local": "npx live-server _site/"
  },
  "type": "commonjs"
}
